[project]
name = "deploy"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "ansible>=13.0.0",
]

[dependency-groups]
dev = [
    "ansible-lint>=25.11.1",
    "typing-extensions==4.14.1",
]
